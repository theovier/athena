package com.theovier.athena.client.ecs.prefabs.loaders.components


class SpineComponentLoaderTest {

    fun isComponentLoadedCorrectly() {
        //TODO, need to get the gdx headless backend in order to fulfill the FileHandleResolver in the Loader
    }

    fun isInitialAnimationLoaded() {
        //TODO, need to get the gdx headless backend in order to fulfill the FileHandleResolver in the Loader
    }
}